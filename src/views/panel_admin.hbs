<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Jitter | Configuración</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <nav class="navbar">
      <div class="nav-container">
        <a href="/panel" class="logo">Jitter</a>
        <div class="nav-right">
          <span class="user-tag">{{user.email}}</span>
          <a href="/panel" class="nav-link">Panel</a>
          <a href="/api/logout" class="nav-link" style="color: #ef4444;">Cerrar sesión</a>
        </div>
      </div>
    </nav>

    <main class="main-content">
      <div class="page-header">
        <h1>Configuración del Sistema</h1>
        <p>Parámetros críticos de la instancia de producción y variables de entorno.</p>
      </div>

      <div class="card">
        <div class="table-wrapper">
          <table class="data-table">
            <thead>
              <tr>
                <th>Clave</th>
                <th>Valor de entorno</th>
                <th>Estado</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td class="config-label">DB_HOST</td>
                <td class="config-value">{{db_host}}</td>
                <td><span class="status-pill pill-online">Activo</span></td>
              </tr>
              <tr>
                <td class="config-label">ENCRYPTION_SECRET</td>
                <td class="config-value flag-highlight">{{flag}}</td>
                <td><span class="status-pill pill-online">Activo</span></td>
              </tr>
              <tr>
                <td class="config-label">API_GATEWAY</td>
                <td class="config-value">{{api_gateway}}</td>
                <td><span class="status-pill pill-online">Activo</span></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <div style="margin-top: 24px;">
        <a href="/panel" style="font-size: 14px; color: #71717a; text-decoration: none;">&larr; Volver al listado de usuarios</a>
      </div>
    </main>

    {{#if rateLimitError}}
      <div style="position: fixed; bottom: 0; left: 0; width: 100%; background: #450a0a; color: #ef4444; padding: 12px; text-align: center; font-size: 13px; font-weight: 600; border-top: 1px solid #7f1d1d;">
        {{rateLimitError}}
      </div>
    {{/if}}
  </body>
</html>